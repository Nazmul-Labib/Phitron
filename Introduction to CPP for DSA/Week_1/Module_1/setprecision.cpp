#include <bits/stdc++.h>
#include <iomanip>
using namespace std;

#define int long long
#define pb push_back
#define all(x) (x).begin(), (x).end()
#define fast_io                       \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL);

int32_t main()
{
    fast_io;
    int t = 1;
    // cin >> t;
    while (t--)
    {
        double d = 1.930937282;
        cout << fixed << setprecision(9) << d << endl;//used to determine how many digits you want to print after decimal point
    }
    return 0;
}