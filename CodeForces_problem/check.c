#include<stdio.h>
#include<stdlib.h>
#include<limits.h>
int main(){

    int count[1000] = {0};
    int n;
    scanf("%d",&n);
    int arr[n];
    for (int k = 0; k < n; ++k) {
        count[arr[k] % d1]++;
    }
    return 0;
}