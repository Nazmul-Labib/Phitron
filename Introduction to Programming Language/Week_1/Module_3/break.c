#include <stdio.h>
#include <stdbool.h>
int main()
{
    // bool a = true;
   // printf("Enter a number:\n");
   // int n;
   // scanf("%d", &n);
    for (int i = 1; i < 10; i++)
    {
        if (i == 3)
        {
            break;
        }
        printf("%d\n", i);
    }
}